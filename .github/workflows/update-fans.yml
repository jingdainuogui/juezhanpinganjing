name: Update Creator Data

on:
  schedule:
    - cron: "0 */6 * * *"   # 每6小时跑一次
  workflow_dispatch:        # 允许手动触发
permissions:
  contents: write
jobs:
  update:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Update fan counts
        shell: pwsh
        run: |
          cd scripts
          powershell -ExecutionPolicy Bypass -File .\update-fans.ps1

      - name: Update tagged works
        run: node scripts/update-portfolio.mjs

      - name: Cache portfolio covers
        run: node scripts/cache-portfolio-covers.mjs

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add data/fans.json
          git add data/fans.js
          git add data/portfolio.js
          git add data/douyin-import.js
          git add assets/portfolio-covers

          git diff --quiet && git diff --staged --quiet || git commit -m "auto: update fans data"

          git push
